# AWS sso configuration
[sso-session example]
sso_region = us-east-1
sso_start_url = https://example.awsapps.com/start

# AWS Credential Helper Configuration
[profile example]
region = us-east-1
credential_process = "<home-path>/.aws/op-cred-helper.sh" <vault> <key>
